<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/titlebar_height"
    android:background="@color/main_theme"
    app:elevation="0dp">

    <ImageButton
        android:id="@+id/titleLeftBtn0"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:background="@null"
        android:src="@drawable/selector_button_back"
        android:onClick="onClick" />

    <TextView
        android:id="@+id/titleTxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:ellipsize="end"
        android:gravity="center"
        android:textSize="20sp"
        android:text="제목"
        android:textColor="@color/white"
        app:fontFamily="@font/my_noto_sans_kr_medium_hestia" />

    <ImageButton
        android:id="@+id/titleRightBtn0"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignWithParentIfMissing="true"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@+id/titleRightBtn1"
        android:paddingStart="10dp"
        android:paddingEnd="5dp"
        android:background="@null"
        android:onClick="onClick"
        android:visibility="invisible" />

    <Button
        android:id="@+id/titleRightBtn1"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:drawableEnd="@drawable/selector_button_logout"
        android:drawablePadding="4.7dp"
        android:paddingEnd="10.3dp"
        android:textSize="13.3sp"
        android:textColor="@color/selector_button_titlebar_text"
        android:text="로그아웃"
        android:background="@null"
        android:onClick="onClick"
        app:fontFamily="@font/my_noto_sans_kr_medium_hestia" />

</RelativeLayout>